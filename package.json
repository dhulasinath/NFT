{
  "name": "nft-credential-verification",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf cache artifacts",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "mint:sepolia": "hardhat run scripts/mint.js --network sepolia",
    "verify:onchain": "hardhat run scripts/verify-onchain.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.4",
    "rimraf": "^5.0.5"
  }
}
